{
  "name": "@seiagentguard/sdk",
  "version": "1.0.0",
  "scripts": {
    "test": "echo \"No tests specified for SDK package in Phase 1\" && exit 0",
    "build": "echo \"SDK build will be added in Phase 4\" && exit 0"
  }
}
