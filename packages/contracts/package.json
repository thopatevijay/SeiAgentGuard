{
  "name": "@seiagentguard/contracts",
  "version": "1.0.0",
  "scripts": {
    "test": "echo \"No tests specified for contracts package in Phase 1\" && exit 0",
    "compile": "echo \"Contract compilation will be added in Phase 3\" && exit 0"
  }
}
