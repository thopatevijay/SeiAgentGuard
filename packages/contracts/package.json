{
  "name": "@seiagentguard/contracts",
  "version": "1.0.0",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy:testnet": "hardhat deploy --network sei-testnet",
    "verify": "hardhat verify --network sei-testnet"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.2.0"
  },
  "devDependencies": {
    "hardhat": "^2.22.17",
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "hardhat-deploy": "^0.12.4"
  }
}
